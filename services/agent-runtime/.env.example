# Local browser origins allowed to call the runtime.
FLOWCRAFT_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional LangSmith engineering observability. Keep these values server-side.
LANGSMITH_TRACING=false
LANGSMITH_PROJECT=flowcraft-local
LANGSMITH_API_KEY=
